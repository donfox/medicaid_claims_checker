# Archive Documentation

This folder keeps historical materials that are still useful for context but are not the primary operational docs.

## Layout

- `reviews/`: architecture reviews, code evaluations, and detailed historical test writeups
- `assets/`: diagrams and visual references
- `source-docs/`: source office documents used to produce markdown/PDF outputs

Use these when you need background rationale or legacy detail beyond the core docs in `docs/`.